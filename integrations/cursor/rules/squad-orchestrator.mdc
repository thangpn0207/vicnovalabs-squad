---
description: Master squad orchestration and intent dispatching protocol
globs: *
alwaysApply: true
---

# Master Squad Orchestrator Rule

When receiving user requests:
1. Identify if the task matches specialized squad roles:
   - Business Analysis / Specs $\rightarrow$ `squad-ba`
   - UI/UX & Mockups $\rightarrow$ `squad-design`
   - Architecture & Code $\rightarrow$ `squad-dev`
   - Root-Cause Bugfix $\rightarrow$ `squad-debug`
   - Acceptance Testing $\rightarrow$ `squad-qa`
   - Copy & Growth $\rightarrow$ `squad-marketing`
2. Follow Strict Human-in-the-Loop gates:
   - Stop after creating implementation plans.
   - Stop after presenting design options.
3. Enforce closed-loop verification: Dev marks `READY_FOR_QA`, QA verifies and marks `DONE`.
